import React from 'react'
import CustomTextField from './CustomTextField'

const InputAddress = ({ register }) => {
    return <CustomTextField label={'Direccion actual'} register={register}></CustomTextField>
}

export default InputAddress
